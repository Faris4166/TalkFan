# Fanclub Web Board 🚀

โปรเจกต์เว็บบอร์ด (Web Board) สำหรับการตั้งกระทู้ พูดคุย แสดงความคิดเห็น และแบ่งปันเรื่องราวต่างๆ ถูกออกแบบมาให้ใช้งานง่าย รวดเร็ว และมี UI ที่ทันสมัย

## 🛠️ เครื่องมือและเทคโนโลยีที่ใช้ (Tech Stack)

ในการสร้างเว็บไซต์นี้ อาศัยการทำงานร่วมกันของเทคโนโลยีต่างๆ ดังนี้:

### Frontend (ส่วนแสดงผลฝั่งผู้ใช้)

- **HTML5 / CSS3**: โครงสร้างและการตกแต่งเว็บพื้นฐาน
- **Tailwind CSS**: Utility-first CSS Framework ที่ช่วยให้การเขียน UI สวยงามและรวดเร็ว ไม่ต้องเขียน CSS ด้วยตัวเองเยอะ
- **DaisyUI**: UI Components สำหรับ Tailwind CSS ช่วยให้สร้างส่วนประกอบของเว็บ (ปุ่ม, การ์ด, ป๊อปอัป, กล่องข้อความ) ได้รวดเร็วและดูเป็นมืออาชีพ
- **JavaScript (jQuery / AJAX)**: จัดการการโต้ตอบกับผู้ใช้ (Interactivity) แบบเรียลไทม์ เช่น การกดไลค์, คอมเมนต์, การยืนยันลบกระทู้ โดยไม่ต้องรีโหลดหน้าเว็บให้ขัดจังหวะผู้ใช้

### Backend (ส่วนจัดการระบบหลังบ้าน)

- **PHP (Vanilla PHP)**: จัดการลอจิกฝั่งเซิร์ฟเวอร์ทั้งหมด เช่น การประมวลผลฟอร์ม, การสร้าง Session เพื่อล็อกอิน, ระบบ Authentication, การป้องกันโจมตี (CSRF) และ File Uploads
- **MySQL / MariaDB**: ระบบจัดการฐานข้อมูล (RDBMS) สำหรับจัดเก็บรายชื่อสมาชิก (users), กระทู้ (posts), และความคิดเห็น (comments)

### Server & Tools (เครื่องมือพัฒนา)

- **XAMPP**: โปรแกรมจำลอง Web Server (Apache) และ Database Server ไว้บนเครื่องคอมพิวเตอร์ของเราสำหรับการพัฒนา

---

## ⚙️ วิธีการติดตั้งและใช้งาน (Installation & Setup)

1. **เตรียมความพร้อม**
   ตรวจสอบว่าในเครื่องมีเว็บเซิร์ฟเวอร์จำลอง เช่น [XAMPP](https://www.apachefriends.org/index.html) หรือซอฟต์แวร์จำลองอื่นๆ ติดตั้งและพร้อมใช้งาน

2. **นำเข้า Source Code**
   คัดลอกหรือ Clone โฟลเดอร์ของโปรเจกต์ `Fanclub` ไปไว้ในโฟลเดอร์ Root ของเซิร์ฟเวอร์ (สำหรับ XAMPP คือ `C:\xampp\htdocs\Fanclub`)

3. **เตรียมฐานข้อมูล (Database)**
   - เปิดโปรแกรม XAMPP Control Panel และกด Start ที่ **Apache** และ **MySQL**
   - เปิดเบราว์เซอร์ไปที่ `http://localhost/phpmyadmin/`
   - สร้างฐานข้อมูล (Database) ใหม่ (เช่นชื่อ `fanclub_db`) โดยตั้งค่า Collation เป็น `utf8mb4_general_ci` เพื่อให้รองรับภาษาไทยได้อย่างสมบูรณ์
   - ไปที่แท็บ **Import** และเลือกอัปโหลดไฟล์ `database.sql` ที่อยู่ในโฟลเดอร์โปรเจกต์ เพื่อสร้างโครงสร้างตารางข้อมูลทั้งหมดอัตโนมัติ

4. **ตั้งค่าการเชื่อมต่อฐานข้อมูล (Database Config)**
   เข้าไปแก้ไขไฟล์ใน `config/db.php` เพื่อตั้งค่าการเชื่อมต่อให้ตรงกับเครื่องของคุณ:

   ```php
   $host = 'localhost';
   $user = 'root'; // Username ของ MySQL
   $pass = '';     // Password ของ MySQL (XAMPP ปกติจะว่างไว้)
   $dbname = 'fanclub_db'; // ชื่อ Database ที่สร้างไว้ในข้อ 3
   ```

5. **เข้าใช้งานเว็บไซต์**
   เปิดเบราว์เซอร์แล้วเข้าไปที่ `http://localhost/Fanclub` เพื่อเริ่มใช้งาน

---

## 🌐 หลักการเขียนคอมเมนต์โค้ดแบบ 2 ภาษา (Bilingual Comments: TH / EN)

ในโปรเจกต์นี้ มีการออกแบบและจัดการโค้ดโดยใช้ "หลักการคอมเมนต์แบบ 2 ภาษา" ซึ่งเป็นโครงสร้างมาตรฐานในการทำงานร่วมกัน ดังนี้:

### ทำไมถึงต้องมีทั้งหน้าคำอธิบายภาษาไทยและอังกฤษ?

1. **[EN] เพื่อความเป็นสากล (Global Standard)**:
   ภาษาอังกฤษช่วยทำให้นักพัฒนาชาวต่างชาติ, AI Tools, หรือเครื่องมือค้นหา สามารถเข้าใจการทำงานของบล็อกโค้ดนั้นได้ (เป็นกฎพื้นฐานของการพัฒนาซอฟต์แวร์ระดับสากล)
2. **[TH] เพื่อประสิทธิภาพของทีมภายในประเทศ (Local Efficiency)**:
   ช่วยให้นักพัฒนาในทีม (คนไทย) หรือผู้ที่เพิ่งมารับช่วงโปรเจกต์ต่อ สามารถทำความเข้าใจลอจิกหรือจุดประสงค์ของฟังก์ชันได้ไวขึ้น ไม่ต้องเสียเวลาแปลศัพท์เฉพาะ (Jargon) ช่วยลดกำแพงเรื่องภาษา

### รูปแบบการนำไปใช้งาน (Best Practice)

เวลาใช้งาน ให้เขียนคอมเมนต์กำกับอธิบายเฉพาะส่วนที่เป็นบล็อกลอจิกสำคัญ คลาส หรือฟังก์ชันที่ทำงานซับซ้อน โดยใช้ Format ควบคู่กันตามลำดับดังรูปด้านล่าง:

- บรรทัดแรก: ระบุแท็ก `[EN]` ตามด้วยคำอธิบายสั้นๆ ภาษาอังกฤษ
- บรรทัดถัดไป: ระบุแท็ก `[TH]` ตามด้วยคำแปลหรือคำอธิบายบริบทในภาษาไทย

**ตัวอย่างในไฟล์ PHP:**

```php
// [EN] Fetch user info and their total post count
// [TH] ดึงข้อมูลส่วนตัวของผู้ใช้ และนับจำนวนกระทู้ทั้งหมดที่เคยตั้ง
$stmt = $conn->prepare("SELECT username, email, profile_img, (SELECT COUNT(*) FROM posts WHERE user_id = ?) as post_count FROM users WHERE id = ?");
$stmt->bind_param("ii", $user_id, $user_id);
$stmt->execute();
```

**ตัวอย่างในไฟล์ JavaScript:**

```javascript
// [EN] Universal AJAX submission logic for both basic comments and threaded replies
// [TH] กลไกส่งความคิดเห็นผ่าน AJAX รองรับทั้งคอมเมนต์หลัก และการตอบกลับใน Thread
$("#commentForm, #replyForm").on("submit", function (e) {
  e.preventDefault();
  // Logic implementation...
});
```

### ข้อควรระวังและคำแนะนำ:

- บอก **"ทำไม (Why) และ ทำอะไร (What)"** ไม่ใช่แค่อธิบายว่าโค้ดทำงาน **"อย่างไร (How)"**
- ไม่ต้องคอมเมนต์ทุกบรรทัด ให้คอมเมนต์กำกับไว้เฉพาะด้านบนสุดของบล็อกโค้ดหลักๆ ให้คนอ่านกวาดสายตาเข้าใจภาพรวมได้ก็พอ
- หากมีการแก้ไขโค้ดใหม่ **ต้องอัปเดต** คำอธิบายคอมเมนต์ทั้ง 2 ภาษาให้ตรงกับโค้ดจริงเสมอ
